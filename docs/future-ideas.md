# 향후 아이디어 (지금 구현 안 함, 잊지 않게 기록만)

## 회원/비회원 차등 알림 (2026-09-04 논의)

- **비회원**: 실시간 알림 대신, 매일 아침 9시에 전날부터 그 시간까지 쌓인 뉴스 중 본인이 등록한 키워드/종목에 해당하는 내용만 정리해서 배치로 발송 (일일 다이제스트)
- **회원**: 실시간으로 알림 수신 (SSE/WebSocket)

프리미엄 전환 유인(회원가입 시 실시간성 제공)으로 활용 가능. 지금은 인증 없이 기초 도메인만 구현하는 단계라 나중에 `User`에 회원 여부 플래그, 배치 발송 스케줄러(Spring Scheduler 등)를 추가하는 방식으로 확장 예정.
